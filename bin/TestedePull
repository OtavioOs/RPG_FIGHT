Este é um teste de "pull" para puxar do github

Att. Feito o comando "git pull origin main" consegui puxar do git.


Tentando outra vez


Para levar ate o repositorio

git add .
git commit -m "Resolva conflitos"
git push origin main


mandando teste de push